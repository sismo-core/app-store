import { SpaceConfig } from "../types";

export const demoSpaceConfig: SpaceConfig = {
  slug: 'demo-space',
  name: 'Demo space',
  description: 'This space is a Demo space to test the Demo env.',
  banner: "banner.png",
  demoEnabled: true
}