import * as config from "../../../../../space-config";

export async function POST(request: Request) {
  // Find / Call the space config that matches the request body

  // Find the Claim and Auth request that matches the request body

  // Initiate the SismoConnect server

      // const sismoConnectConfig: SismoConnectServerConfig = {
      //   appId: "0x112a692a2005259c25f6094161007967",
      // };
      
      // const sismoConnect = SismoConnect(sismoConnectConfig);


  // Verify the request
      //  const result: SismoConnectVerifiedResult = await sismoConnect.verify(
      //   response,
      //   {
      //     // the request object
      //   }
      // );

  // call the service for the output "google_sheet"
      //

  //respond the templated response
}